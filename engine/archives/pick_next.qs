quando.pick.one reset="", inverted=false, callback-{
    quando.log text="a"
    quando.log text="b"
    quando.log text="c"
}

quando.pick.one reset="", inverted=true, callback-{
    quando.log text="a"
    quando.log text="b"
    quando.log text="c"
}