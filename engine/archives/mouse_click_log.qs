quando.mouse.handleClick key="left" callback={
    quando.log text="clicked"
}

quando.mouse.handleClick key="right" callback={
    quando.log text="clicked"
}

quando.mouse.handleClick key="middle" callback={
    quando.log text="clicked"
}

quando.mouse.handleClick key="acc1" callback={
    quando.log text="clicked"
}

quando.mouse.handleClick key="acc2" callback={
    quando.log text="clicked"
}