quando.mouse.handleY pressed=false, inverted=false callback={
      quando.log text="here"
}

quando.mouse.handleX pressed=false, inverted=false callback={
      quando.log text="here"
}

quando.mouse.handleY pressed=true, inverted=false callback={
      quando.log text="here"
}

quando.mouse.handleY pressed=false, inverted=true callback={
      quando.log text="here"
}