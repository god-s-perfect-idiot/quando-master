quando.time.vary count=2, units="seconds", mode="seesaw", times=4, timesUnits="seconds", inverted=false, callback={
    quando.control.mouseX x=val
}

quando.time.vary count=2, units="seconds", mode="seesaw", times=4, timesUnits="seconds", inverted=false, callback={
    quando.control.mouseY y=val
}

quando.time.vary count=2, units="seconds", mode="seesaw", times=4, timesUnits="seconds", inverted=false, callback={
    quando.control.mouse x=val, y=val
}