quando.time.every count=1, units="seconds", callback={
    quando.pick.one reset="", inverted=true, callback={
        quando.log text="a"
        quando.log text="b"
        quando.log text="c"
    }
}

quando.time.every count=1, units="seconds", callback={
    quando.pick.one reset="", inverted=true, callback={
        quando.log text="a"
        quando.log text="b"
        quando.log text="c"
    }
}